{% trans_default_domain 'KreyuDataTable' %}

{% if vars.translation_domain is not same as false %}
    {{- vars.label|trans(vars.label_translation_parameters, vars.translation_domain) -}}
{% else %}
    {{- vars.label -}}
{% endif %}
