{% trans_default_domain 'KreyuDataTable' %}

{% set template = '@KreyuDataTable/data_table_theme.html.twig' %}

{% if block(vars.block_name, template) is defined %}
    {{- block(vars.block_name, template) -}}
{% else %}
    {{- vars.value -}}
{% endif %}
